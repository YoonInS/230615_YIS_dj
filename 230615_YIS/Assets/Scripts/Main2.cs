using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Main2 : MonoBehaviour
{
    List<Character> characterList;    
    void Start()
    {
        characterList = new List<Character>();

       // CreateCharacter(new Character("ashe", "archer",100,30));
       // CreateCharacter(new Character("ashe", "archer", 80, 20));
       // CreateCharacter(new Character("ashe", "archer", 70, 100));

       // RemoveCharacter(2);
       // CreateCharacter(new Character("ashe", "archer", 300, 10));
    }

    private void RemoveCharacter(Character c)
    {
        
    }

    private void CreateCharacter(int index)
    {
       
    }

    void Update()
    {
        
    }
}
